#elf format

The last step of compiling is to call the linker to link object files together. Standalone object files are pure code, it's the linker that synthesize them together, handling reloations and produce the final binary format that is accepted by the operating system.

The final executable format is platform specific, and there will be more
